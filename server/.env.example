# Database Configuration
# For PostgreSQL (production): Get this from Render PostgreSQL dashboard
# Format: postgresql://user:password@host:port/dbname
DATABASE_URL=postgres://user:password@host:port/dbname

# Secret Key
# Generate a long random string for production
# You can use: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=some-long-random-string-change-in-production

# Frontend URL (for CORS)
# Set this to your frontend deployment URL
FRONTEND_URL=https://your-frontend-onrender.com

# Force HTTPS (set to 1 in production)
FORCE_HTTPS=1

# Flask Environment
FLASK_ENV=production

# Legacy support (optional)
FLASK_SECRET=some-long-random-string-change-in-production
ALLOWED_ORIGIN=https://favvtech.github.io,https://votingapp.ibaraysas.com
